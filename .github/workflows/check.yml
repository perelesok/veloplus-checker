name: VeloPlus Check

on:
  schedule:
    - cron: '0 * * * *'  # каждый час
  workflow_dispatch:  # ручной запуск

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check availability and notify
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          check_url() {
            local label="$1"
            local url="$2"
            
            # Fetch with error handling
            http_code=$(curl -s -o /tmp/response.json -w "%{http_code}" "$url")
            
            # Check HTTP status
            if [ "$http_code" != "200" ]; then
                printf "⚠️ <b>[%s]</b> — ошибка HTTP %s" "$label" "$http_code"
                return
            fi
            
            response=$(cat /tmp/response.json)
            
            # Check if response is valid JSON array
            if ! echo "$response" | jq -e 'type == "array"' > /dev/null 2>&1; then
                printf "⚠️ <b>[%s]</b> — неверный формат ответа" "$label"
                return
            fi
            
            # Check if array is empty
            array_length=$(echo "$response" | jq 'length')
            if [ "$array_length" == "0" ]; then
                printf "⚠️ <b>[%s]</b> — пустой ответ от API" "$label"
                return
            fi
            
            # Check if objects have expected fields
            if ! echo "$response" | jq -e '.[0] | has("availability") and has("site")' > /dev/null 2>&1; then
                printf "⚠️ <b>[%s]</b> — изменился формат API" "$label"
                return
            fi
            
            available=$(echo "$response" | jq -r '[.[] | select(.availability != "UNAVAILABLE")] | 
                if length == 0 then 
                    empty 
                else 
                    .[] | "  • \(.site.name): \(.availability) - \(.firstAvailableSlot)"
                end')
            
            if [ -n "$available" ]; then
                printf "✅ <b>[%s]</b>\n%s" "$label" "$available"
            else
                printf "❌ <b>[%s]</b> — нет доступных" "$label"
            fi
          }

          r1=$(check_url "153" 'https://boutique.veloplus-m.fr/pu/sites/stock?offer_quantities=%7B%22153%22%3A1%7D')
          r2=$(check_url "201" 'https://boutique.veloplus-m.fr/pu/sites/stock?offer_quantities=%7B%22201%22%3A1%7D')
          r3=$(check_url "202" 'https://boutique.veloplus-m.fr/pu/sites/stock?offer_quantities=%7B%22202%22%3A1%7D')
          r4=$(check_url "210" 'https://boutique.veloplus-m.fr/pu/sites/stock?offer_quantities=%7B%22210%22%3A1%7D')

          message="🚲 VeloPlus статус:

${r1}

${r2}

${r3}

${r4}"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${message}" \
            -d parse_mode="HTML"
